
import 'dart:ui';

class TransactionModel{
  final Color color, backColor;
  final String title, content, amount, icon;

  TransactionModel(
    this.backColor,
    this.color,
    this.title,
    this.amount,
    this.content,
    this.icon
  );

}